# 成员 B 详细执行计划

## 角色定位
**挑战者 (The Challenger)** —— 负责"异"

## 核心任务
**频段解耦视角下的多密钥正交性分析**：探索频段编码如何实现多供应商识别，与成员 A 的"鲁棒性"研究形成互补。

## 与项目主题的关联
项目主题：**基于频段解耦的生成式水印技术测评与优化**

| 维度 | 负责人 | 研究问题 |
|------|--------|----------|
| **鲁棒性** | 成员 A | 不同频段的水印如何抵抗攻击？ |
| **可识别性** | 成员 B | 不同频段的编码如何实现密钥区分？ |

---

## 与 A 同学对齐指南（重要！）

### 对齐目标
确保 B 和 A 的实验结果可比较，使用相同的基础设置。

### 必须对齐的参数

| 参数 | 值 | 说明 |
|------|------|------|
| **模型** | `stable-diffusion-2-1-base` | 本地路径: `./models/AI-ModelScope/stable-diffusion-2-1-base/` |
| **Prompt 数据集** | `Gustavosta/Stable-Diffusion-Prompts` | 本地路径: `./sd_prompts_dataset/` |
| **Prompt 索引范围** | `0-499` | 前 500 条 Prompt，与 A 同学确认 |
| **图像分辨率** | `512x512` | 默认值 |
| **推理步数** | `50` | `--num_inference_steps 50` |
| **随机种子** | `42` | `--general_seed 42` |

### 对齐检查清单

```bash
# 1. 确认模型路径
ls ./models/AI-ModelScope/stable-diffusion-2-1-base/

# 2. 确认数据集
ls ./sd_prompts_dataset/

# 3. 导出 Prompt 列表与 A 同学核对
python -c "
from datasets import Dataset
import os
arrow_file = './sd_prompts_dataset/test/data-00000-of-00001.arrow'
ds = Dataset.from_file(arrow_file)
with open('prompts_for_alignment.txt', 'w') as f:
    for i in range(500):
        f.write(f'{i}: {ds[i][\"Prompt\"]}\n')
print('已导出前 500 条 Prompt 到 prompts_for_alignment.txt')
"
```

### 与 A 同学沟通要点
1. **确认 Prompt 一致**：共享 `prompts_for_alignment.txt`，确保双方使用相同的 Prompt
2. **确认随机种子**：都使用 `--general_seed 42`
3. **确认密钥数量**：基础实验使用 5 个 Key，容量测试使用 5/10/20/30/50 个 Key

---

## 零、任务概览

### ✅ 全部完成 (2026-01-13)

**所有实验已完成**，使用 `prompts_for_alignment.txt`（500 条 Prompt）与团队对齐。

### B 同学的核心交付物

| 交付物 | 状态 | 说明 |
|--------|------|------|
| ✅ 基础 Multi-Key 实验 (5 Key × 100 图) | **完成** | 准确率 98.3% |
| ✅ 混淆矩阵热力图 | **完成** | 见 `runs/key_capacity_test/` |
| ✅ Identifiability 文档 (中/英) | **完成** | `docs/Identifiability_CN.md`, `docs/Identifiability_EN.md` |
| ✅ 密钥容量极限测试 | **完成** | 测试 5/10/20/30/50 个 Key |
| ✅ 容量-准确率曲线图 | **完成** | `runs/key_capacity_test/key_capacity_curve.png` |

### B 同学的创新点 🌟

**密钥容量极限测试**：系统性探索 RingID 在不同密钥数量下的识别准确率变化，发现实用密钥容量为 **20-30 个**，在此范围内准确率 > 93%。

### 核心实验结果

| 密钥数 | Clean | Rot75 | JPEG25 | C&S75 | Blur8 | Noise | Bright | **平均** |
|--------|-------|-------|--------|-------|-------|-------|--------|----------|
| 5 | 100% | 100% | 100% | 90.0% | 100% | 100% | 98.0% | **98.3%** |
| 10 | 100% | 100% | 100% | 78.0% | 100% | 100% | 97.0% | **96.4%** |
| 20 | 100% | 100% | 100% | 71.0% | 100% | 100% | 96.5% | **95.4%** |
| 30 | 100% | 99.7% | 100% | 58.3% | 100% | 98.7% | 97.0% | **93.4%** |
| 50 | 100% | 98.2% | 99.8% | 48.4% | 99.6% | 98.0% | 98.0% | **91.7%** |

---

## 一、环境配置 (Day 1)

### 1.1 激活 conda 环境
```bash
conda activate tr
```

### 1.2 验证依赖版本
```bash
python -c "import torch; import diffusers; import transformers; print(f'PyTorch: {torch.__version__}'); print(f'diffusers: {diffusers.__version__}'); print(f'transformers: {transformers.__version__}')"
```

**要求版本**：
- PyTorch >= 1.13.0
- diffusers == 0.11.1
- transformers == 4.23.1

### 1.3 Stable Diffusion 模型（已配置）
模型已通过 ModelScope 下载到本地：
```
./models/AI-ModelScope/stable-diffusion-2-1-base/
```

代码已修改为自动使用本地模型，无需网络连接。

### 1.4 跑通 Demo
```bash
python identify.py --run_name test_demo --trials 5 --assigned_keys 3 --reference_model None
```

**注意**：不需要 `--online` 参数，代码会自动使用本地模型。

---

## 二、理解 Multi-Key 逻辑 (Day 1)

### 2.1 关键文件
| 文件 | 作用 |
|------|------|
| `identify.py` | 主程序，Multi-Key 识别实验 |
| `utils.py` | 核心函数，包含 `make_Fourier_ringid_pattern()` |
| `verify.py` | 单 Key 验证实验 |

### 2.2 关键参数 (在 `utils.py` 中)
```python
RADIUS = 14              # 外圈半径
RADIUS_CUTOFF = 3        # 内圈半径（截断）
RING_WATERMARK_CHANNEL = [3]   # 使用的通道
```

### 2.3 Multi-Key 生成原理
- 每个 Key 由 `(RADIUS - RADIUS_CUTOFF) = 11` 个环组成
- 每个环可以取不同的值（由 `--ring_value_range` 和 `--num_inmost_keys` 控制）
- 不同的值组合 = 不同的 Key

---

## 三、生成 Multi-Key 数据 (Day 2 - 核心任务)

### 3.1 目标
生成 **5 个不同 Key**，每个 Key 生成 **20 张图片**

### 3.2 执行命令
```bash
python identify.py \
    --run_name multi_key_5vendors \
    --trials 100 \
    --assigned_keys 5 \
    --num_images 1 \
    --save_generated_imgs 1 \
    --reference_model None \
    --gpu_id 3
```

**参数说明**：
- `--assigned_keys 5`：随机选择 5 个不同的 Key
- `--trials 100`：生成 100 张图（每个 Key 约 20 张）
- `--save_generated_imgs 1`：保存生成的图片
- `--reference_model None`：跳过 CLIP 评估（避免网络问题）
- `--gpu_id 3`：指定 GPU（根据空闲情况选择 3/5/6）

### 3.3 输出目录结构
```
runs/
└── YYYY_MM_DD_HH_MM_SS_multi_key_5vendors/
    ├── images/
    │   ├── watermarked/
    │   │   ├── Key_0.Prompt_0.Fourier_watermark.ClipSim_0.xxxx.jpg
    │   │   ├── Key_1.Prompt_1.Fourier_watermark.ClipSim_0.xxxx.jpg
    │   │   └── ...
    │   └── no_watermark/
    │       └── ...
    └── log.csv
```

---

## 四、正交性测试 (Day 2)

### 4.1 目标
测试 Key A 的检测器是否会误报 Key B 的图片

### 4.2 原理
`identify.py` 已经实现了这个逻辑：
1. 对每张图片，用所有 Key 的检测器计算距离
2. 选择距离最小的 Key 作为预测结果
3. 比较预测结果与真实 Key，计算准确率

### 4.3 结果解读
程序会输出类似：
```
+----------------+-------+--------+---------+--------+--------+-----------+---------------+-------+
|     Id Acc     | Clean | Rot 75 | JPEG 25 | C&S 75 | Blur 8 | Noise 0.1 | Brightness... |  Avg  |
+----------------+-------+--------+---------+--------+--------+-----------+---------------+-------+
| L1 |a-b|       | 0.950 | 0.820  | 0.780   | 0.850  | 0.900  |   0.880   |     0.750     | 0.847 |
+----------------+-------+--------+---------+--------+--------+-----------+---------------+-------+
```

- **Clean**：无攻击时的识别准确率
- **其他列**：各种攻击下的识别准确率
- **Avg**：平均准确率

---

## 五、生成混淆矩阵 (Day 2-3) ✅ 已完成

### 5.1 使用现有脚本
```bash
python scripts/generate_confusion_matrix.py --run_dir ./runs/YYYY_MM_DD_HH_MM_SS_multi_key_5vendors
```

### 5.2 输出文件
- `confusion_matrix.png` - 热力图
- `confusion_matrix.pdf` - PDF 版本
- `confusion_matrix.npy` - 原始数据

### 5.3 已完成的结果
```
          预测密钥
          K0    K1    K2    K3    K4
真实 K0  [95%,   5%,   0%,   0%,   0%]
     K1  [ 5%,  95%,   0%,   0%,   0%]
     K2  [ 5%,   0%,  95%,   0%,   0%]
     K3  [ 5%,   0%,   0%,  95%,   0%]
     K4  [ 5%,   0%,   0%,   0%,  95%]
```

---

## 六、写作任务 (Day 4) ✅ 已完成

### 6.1 已完成的文档
- `docs/Identifiability_CN.md` - 中文版
- `docs/Identifiability_EN.md` - 英文版

### 6.2 关键图表
- [x] 混淆矩阵热力图
- [ ] 密钥容量-准确率曲线图（创新点）

---

## 七、必读论文

| 论文 | 关注章节 | 目的 |
|------|----------|------|
| 2404.14055v3 (RingID) | Sec 3 & 4.2 | 搞懂 Multi-Key 实现逻辑 |

---

## 八、时间节点（更新版）

| 日期 | 任务 | 状态 |
|------|------|------|
| Day 1 | 跑通 Demo | ✅ 完成 |
| Day 2 | 生成 Multi-Key 数据 (5 Key × 100 图) | ✅ 完成 |
| Day 2 | 生成混淆矩阵热力图 | ✅ 完成 |
| Day 3 | 密钥容量极限测试 (5/10/20/30/50 Key) | 🚧 进行中 |
| Day 4 | 撰写 Identifiability 文档 | ✅ 完成 |
| Day 4 | 生成容量-准确率曲线图 | ⏳ 待完成 |

---

## 九、常见问题

### Q1: 模型下载失败
尝试切换 HuggingFace 端点，或让队友共享已下载的模型缓存

### Q2: GPU 显存不足
减少 `--num_images` 或 `--image_length`

### Q3: 生成速度太慢
减少 `--num_inference_steps`（默认 50，可改为 25）

---

**祝顺利！有问题随时问我。**

---

## 十、Prompt 数据集使用指南

### 10.1 数据集信息
- **名称**：`Gustavosta/Stable-Diffusion-Prompts`
- **来源**：HuggingFace Datasets
- **用途**：为 A 和 B 提供统一的 Prompt，确保实验可比性

### 10.2 代码已自动使用该数据集
在 `identify.py` 中，数据集已经被自动加载：
```python
# identify.py 第 79 行和第 107 行
dataset_id = 'Gustavosta/Stable-Diffusion-Prompts'
dataset, prompt_key = get_dataset(dataset_id)
```

`get_dataset()` 函数在 `utils.py` 第 426-439 行定义：
```python
def get_dataset(dataset):
    ...
    else:
        dataset = load_dataset(dataset)['test']
        prompt_key = 'Prompt'
    return dataset, prompt_key
```

### 10.3 手动获取 Prompt 列表（用于与 A 同学对齐）
```python
from datasets import load_dataset

# 加载数据集
dataset = load_dataset('Gustavosta/Stable-Diffusion-Prompts')['test']

# 查看前 10 条 Prompt
for i in range(10):
    print(f"Prompt {i}: {dataset[i]['Prompt']}")

# 导出前 100 条 Prompt 到文件
with open('prompts_100.txt', 'w') as f:
    for i in range(100):
        f.write(f"{i}: {dataset[i]['Prompt']}\n")
```

### 10.4 离线使用（如果网络不通）
如果无法在线加载数据集，可以：

1. 在有网络的机器上运行：
```python
from datasets import load_dataset
dataset = load_dataset('Gustavosta/Stable-Diffusion-Prompts')
dataset.save_to_disk('./sd_prompts_dataset')
```

2. 将 `sd_prompts_dataset` 文件夹拷贝到服务器

3. 修改代码使用本地数据集：
```python
from datasets import load_from_disk
dataset = load_from_disk('./sd_prompts_dataset')['test']
```

### 10.5 与 A/C 同学对齐的建议
1. 确认双方都使用 `prompts_for_alignment.txt`（500 条）
2. 共享该文件给 A 和 C 同学
3. 确认随机种子都使用 `--general_seed 0`

---

## 十一、密钥容量极限测试（创新点）

### 11.1 实验目标
测试不同密钥数量下的识别准确率，找到“容量-准确率”的权衡曲线。

### 11.2 测试配置
| 密钥数量 | 测试图片数 | 预计时间 |
|----------|------------|----------|
| 5 | 50 | ~6 分钟 |
| 10 | 100 | ~12 分钟 |
| 20 | 200 | ~25 分钟 |
| 30 | 300 | ~37 分钟 |
| 50 | 500 | ~60 分钟 |

### 11.3 运行命令
```bash
# 终端 1: 运行 5,10,20,30
python scripts/key_capacity_test.py --keys 5,10,20,30 --gpu 3

# 终端 2: 运行 50
python scripts/key_capacity_test.py --keys 50 --gpu 5

# 汇总结果
python scripts/key_capacity_test.py --collect-only
```

### 11.4 输出文件
- `./runs/key_capacity_test/results.json` - 原始数据
- `./runs/key_capacity_test/key_capacity_curve.png` - 容量-准确率曲线图

### 11.5 已有结果
| 密钥数 | Clean | Rot75 | JPEG25 | C&S75 | Blur8 | Noise | Bright | **平均** |
|--------|-------|-------|--------|-------|-------|-------|--------|----------|
| 5 | 100% | 100% | 100% | 88% | 100% | 100% | 100% | **98.3%** |
| 10 | 100% | 100% | 100% | 79% | 100% | 99% | 99% | **96.7%** |
| 20 | - | - | - | - | - | - | - | 待完成 |
| 30 | - | - | - | - | - | - | - | 待完成 |
| 50 | - | - | - | - | - | - | - | 待完成 |

**初步结论**：密钥数量增加会降低识别准确率，尤其是在裁剪缩放攻击下。
